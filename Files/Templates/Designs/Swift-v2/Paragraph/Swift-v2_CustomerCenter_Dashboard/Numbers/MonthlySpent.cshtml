@inherits ViewModelTemplate<OrderListViewModel>
@using Dynamicweb.Rendering
@using Dynamicweb.Frontend
@using Dynamicweb.Ecommerce.Frontend

<div class="fs-3 fw-bold text-price">
    @if(Model?.Orders?.Count > 0 && Model.Orders.Select(o => o.Price.CurrencyCode).Distinct().Count() == 1)
    {
        @((Model?.Orders?.Sum(o => o.Price.Price) ?? 0).ToString("C"))
    }
    else 
    {
        @((Model?.Orders?.Sum(o => o.Price.Price) ?? 0).ToString("C")) //zero value formatted
    }
</div>