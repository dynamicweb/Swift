@inherits Dynamicweb.Rendering.ViewModelTemplate<ParagraphViewModel>
@using Dynamicweb.Frontend

@{
    AddScript("/Files/Templates/Designs/Swift-v2/Paragraph/Swift-v2_CustomerCenter_Dashboard/Assets/widget.js");
    var widgetTitle = Model?.Item?.GetString("Title") ?? "Pending quotes";
    var baseUrl = Model?.Item?.TryGetLink("BaseLink", out var baseLink) ?? false ? baseLink.Url : null;
}

<div class="card h-100" style="--swift-content-padding: 1.25rem;">
    <div class="card-body">
        <div class="hstack flex-wrap gap-2 mb-2">
            <div class="d-grid p-2 rounded-2" style="background-color: color-mix(in srgb, var(--bs-orange) 10%, transparent);">
                <span class="icon fs-5" style="color: var(--bs-orange);">@ReadFile("/Files/Images/Icons/file-lines.svg")</span>
            </div>
            <span class="fs-7 fw-semibold text-body text-opacity-75">@(Translate(widgetTitle))</span>
        </div>
        <div class="fs-3 fw-bold lh-1" data-swift-htmx-widget="@(Model?.ID)"
            hx-get="/dwapi/ecommerce/orders/search?Type=quote&PageSize=999" 
            hx-headers='{"x-dw-template": "/Designs/Swift-v2/Paragraph/Swift-v2_CustomerCenter_Dashboard/ResponseTemplates/Mini_Quotes.cshtml", "accept": "text/html"}'
            hx-vals='{"baseUrl": "@(baseUrl)"}'
            hx-ext="swift-auth"
            hx-trigger="swift:load once"
            hx-swap="innerHTML">
            <div class="spinner-border spinner-border-sm" role="status">
                <span class="visually-hidden">Loading...</span>
            </div>
            
        </div>
    </div>
</div>