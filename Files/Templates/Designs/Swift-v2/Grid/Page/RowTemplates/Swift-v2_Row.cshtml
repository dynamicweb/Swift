@inherits Dynamicweb.Rendering.ViewModelTemplate<Dynamicweb.Frontend.GridRowViewModel>

@{
	bool columnsHasOutput = Model.Columns.Any(c => !string.IsNullOrWhiteSpace(c.Output())) || Pageview.IsVisualEditorMode;
}

@if (columnsHasOutput) {
	if (!string.IsNullOrEmpty(Model.ColorScheme?.Id)) {
		<section class="py-6" data-dw-colorscheme="@Model.ColorScheme?.Id" data-swift-gridrow>
			<div class="grid container-xl">
				@foreach(var column in Model.Columns)
				{
					<div class="g-col-12 g-col-lg-@column.ColumnWidth" >
						@column.Output()
					</div>
				}
			</div>
		</section>
	}
	else
	{
		<section class="grid container-xl py-6" data-swift-gridrow>
			@foreach(var column in Model.Columns)
			{
				<div class="g-col-12 g-col-lg-@column.ColumnWidth">
					@column.Output()
				</div>
			}
		</section>
	}
}

